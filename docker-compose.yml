version: "3.8"

services:
  pipeline:
    build: .
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env
    environment:
      - AWS_REGION=us-east-2
    command: python airflow/dags/sales_pipeline_dag.py